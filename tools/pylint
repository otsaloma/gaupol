#!/bin/sh

DIRNAME=$(dirname "$0")
cd "$DIRNAME/.." || exit 1

pylint -f parseable "$@" \
 | egrep -v "Instance of 'ConfigurationStore' has no '[a-z_]*' member" \
 | egrep -v "Method '[a-z_]*' has no 'setter' member"
